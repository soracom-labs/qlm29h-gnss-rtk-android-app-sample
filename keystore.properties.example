# Copy this file to keystore.properties and replace every value locally.
# keystore.properties and common keystore extensions are excluded from Git.
storeFile=/absolute/path/to/company-release-key.jks
storePassword=REPLACE_WITH_SECRET
keyAlias=REPLACE_WITH_ALIAS
keyPassword=REPLACE_WITH_SECRET
